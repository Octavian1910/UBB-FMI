#include <stdlib.h>


int main()
{
    int nrElement = 0;
    scanf_f("%d",&nrElement);
    int* elems = malloc(sizeof(int*nrEelement));

    for (int i = 0 ; i < nrElement; ++i)
    {
        elems[i]= i*i;
    }

    int *pi = malloc(sizeof(int));
    *pi = 5;
    return 0;
}